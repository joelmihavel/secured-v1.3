/** INR, no fractions — matches landlord-facing offer UI. */
export function formatCurrencyInr(n: number): string {
  return new Intl.NumberFormat("en-IN", {
    style: "currency",
    currency: "INR",
    maximumFractionDigits: 0,
  }).format(n);
}
